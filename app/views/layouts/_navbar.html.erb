<span class="navbar-text">
    <% if player_signed_in? %>
    <div> Welcome <%= current_player.username %> </div>
    <%= button_to "Sign out", destroy_player_session_path, method: :delete %>
    <% else %>
        <%= button_to "Sign in", new_player_session_path %>
    <% end %>
   </span>