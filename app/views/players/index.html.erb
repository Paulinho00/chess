<%= render "layouts/navbar" %>
<p style="color: green"><%= notice %></p>

<h1>Players</h1>
<p class="text-center">Dziala to gowno?</p>
<div id="players">
  <% @players.each do |player| %>
    <%= render player %>
    <p>
      <%= link_to "Show this player", player %>
    </p>
  <% end %>
</div>

<%= link_to "New player", new_player_path %>
