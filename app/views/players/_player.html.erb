<%= stylesheet_link_tag "player", media: 'all' %>
<div id="<%= dom_id(player) %>" class="player-info">
  <p>
    <strong><%= player.player_id %></strong>
    <strong>Email:</strong>
    <%= player.email %>
  </p>

  <p>
    <strong>Username:</strong>
    <%= player.username %>
  </p>
</div>
